# Issue 11: redb storage

Assistant: OpenAI Codex.

## Human request

Work on https://github.com/localthought/reflector-rs/issues/11, create a PR,
and merge it when green.

The issue requests AtomicServer's persistent redb Storelike, an environment
variable for its location, macOS setup instructions, and a separate drive
named for the GitHub repository instead of writing into the user's main drive.

## Changes and results

- Replaced the binary's memory store and JSON-AD export with AtomicServer's
  redb Db, configured by STORE_DIR.
- Added repository drives, optional DRIVE_OWNER access grants, and parent/drive
  metadata for imported records and nested resources.
- Retained stored CRDT history during updates and excluded database metadata
  from sync-engine records.
- Documented exclusive file access, the macOS directory, existing JSON exports,
  and rebuilding AtomicServer's separate search index after direct imports.
- Added GitHub CI and tests for configuration, drive assignment, persistence
  across reopening, CRDT history, permissions, deletion, and isolation.
- Reproduced the missing-drive bug before fixing it. All 26 tests, formatting,
  and Clippy passed locally. The PR is to merge only after GitHub CI passes.

No credentials or private issue data were used in tests. The user's running
AtomicServer and its database were not modified. Changes received automated
review and tests; no separate human code review is claimed.
